# Auto-Bot Lite configuration — by Aditya Bhagat
# Maps Salesforce Account names to 3-letter client codes; the engine picks the
# first dbconfig.json environment starting with that code (e.g. EQC -> EQCU_HD_DEV17).
# Add rows as cases arrive. Override per run with: autobot.py solve <case> --env <ENV>

[clients]
EQT Corporation = EQC
